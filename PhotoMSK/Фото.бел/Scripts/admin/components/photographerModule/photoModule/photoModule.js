"use strict";

angular.module("photo.bel.admin.photo", [
        "ngRoute",
        "photo.bel.admin.directivies"
])
    .config([
        "$routeProvider", function ($routeProvider) {
        }
    ]);